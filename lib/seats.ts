// Shared seat-availability numbers for the Đà Nẵng and Foundation classes.
// Both classes are run as the same cohort/waitlist, so they share one seat count.
export const SEATS_TOTAL = 15;
export const SEATS_TAKEN = 13;
